# ============================================================
# 策略基本信息
# ============================================================
strategy:
  name: "MT5 黄金区间突破策略"
  description: "基于前N根K线高低点的区间突破策略，适用于XAUUSD黄金H1周期。"
  author: "backtrader_web"
  category: "simulate"

# ============================================================
# 品种信息
# ============================================================
symbol:
  code: "XAUUSD"
  name: "黄金"
  exchange: "MT5"
  multiplier: 100
  margin_rate: 0.01

# ============================================================
# 策略参数
# ============================================================
params:
  lookback: 20
  volume: 0.01
  stop_loss_pips: 200
  take_profit_pips: 400

# ============================================================
# 数据配置
# ============================================================
data:
  symbol: "XAUUSD"
  data_type: "otc"
  exchange: "MT5"
  timeframe: "15s"
  warmup_bars: 100

# ============================================================
# 模拟交易配置
# ============================================================
simulate:
  initial_cash: 10000
  commission: 0.0001
  slippage: 0.0001
  position_size: 0.01
  duration_seconds: 86400
  session_timeout: 86500

# ============================================================
# 风控配置
# ============================================================
risk:
  max_position: 0.1
  max_loss_per_trade: 0.02
  max_drawdown: 0.15

# ============================================================
# MT5 连接配置
# ============================================================
mt5:
  login: "5047785364"
  password: "NyCh-i4r"
  ws_uri: "wss://web.metatrader.app/terminal"
  symbol_suffix: ""

# ============================================================
# Gateway 配置
# ============================================================
gateway:
  enabled: true
  provider: "mt5_gateway"
  exchange_type: "MT5"
  asset_type: "OTC"

# ============================================================
# 实盘参数
# ============================================================
live:
  symbol: "XAUUSD"
  bar_seconds: 15
  duration_seconds: 86400
  session_timeout: 86500
