# 策略基本信息
strategy:
  name: "EMA双均线交叉策略"
  description: "基于双EMA交叉的多周期交易策略，使用5分钟和日线数据进行过滤"
  author: "yunjinqi"

# 策略参数
params:
  fast_period: 80    # 快线EMA周期
  slow_period: 200   # 慢线EMA周期
  short_size: 2      # 空头仓位规模
  long_size: 1       # 多头仓位规模

# 数据配置
data:
  data_type: "stock"  # 股票数据
  timeframes:
    - "5min"          # 5分钟数据（主周期）
    - "day"           # 日线数据（过滤用）

# 回测配置
backtest:
  initial_cash: 100000    # 初始资金
  coc: true               # Cheat On Close - 收盘价执行
# ============================================================
# CTP 连接配置（用于模拟盘，请填写 CTP 模拟账号）
# ============================================================
ctp:
  broker_id: "3070"
  investor_id: "333306116"
  user_id: "333306116"
  password: "199057"
  app_id: "client_wtyj_1.0.9.9"
  auth_code: "VCSX4A2S43I4RN25"
  user_product_info: "wtpy"
  interface_product_info: "wtpy"
  fronts:
    telecom:
      td_address: "tcp://101.230.79.235:32205"
      md_address: "tcp://101.230.79.235:32213"

# ============================================================
# CTP 模拟盘会话参数
# ============================================================
live:
  symbol: "rb2504"
  network: "telecom"
  bar_seconds: 60
  duration_seconds: 3600
  session_timeout: 3700
  min_price_tick: 1.0
  max_order_size: 1
  contract_metadata:
    rb2504:
      min_price_tick: 1.0
      max_order_size: 1
