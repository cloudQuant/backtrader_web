# BB_ADX Bollinger Bands + ADX mean reversion strategy configuration
# 策略基本信息
strategy:
  name: "布林带ADX均值回归策略"
  description: "使用布林带提供均值回归信号，ADX用于识别和避免趋势市场。仅在ADX低于阈值时执行均值回归交易。"
  author: "backtrader-backtests"

# 策略参数
params:
  BB_MA: 20        # 布林带均线周期
  BB_SD: 2         # 布林带标准差倍数
  ADX_Period: 14   # ADX周期
  ADX_Max: 40      # ADX阈值（超过则避免交易）

# 数据配置
data:
  symbol: "sh600000"
  data_type: "stock"  # stock

# 回测配置
backtest:
  initial_cash: 100000
  commission: 0.001
# ============================================================
# CTP 连接配置（用于模拟盘，请填写 CTP 模拟账号）
# ============================================================
ctp:
  broker_id: "3070"
  investor_id: "333306116"
  user_id: "333306116"
  password: "199057"
  app_id: "client_wtyj_1.0.9.9"
  auth_code: "VCSX4A2S43I4RN25"
  user_product_info: "wtpy"
  interface_product_info: "wtpy"
  fronts:
    telecom:
      td_address: "tcp://101.230.79.235:32205"
      md_address: "tcp://101.230.79.235:32213"

# ============================================================
# CTP 模拟盘会话参数
# ============================================================
live:
  symbol: "rb2504"  # CTP仅支持期货，回测为非期货时请按需修改
  network: "telecom"
  bar_seconds: 60
  duration_seconds: 3600
  session_timeout: 3700
  min_price_tick: 1.0
  max_order_size: 1
  contract_metadata:
    rb2504:
      min_price_tick: 1.0
      max_order_size: 1
