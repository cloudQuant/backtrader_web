# 迭代136：国际化与用户体验提升计划

## 1. 背景与目标

基于迭代131-135的持续优化，项目核心功能已基本完善。本轮迭代聚焦于：

1. **国际化(i18n)**：实现多语言支持，扩大用户群体
2. **移动端适配**：优化响应式布局，提升移动用户体验
3. **用户体验优化**：改进交互细节，提升操作效率
4. **功能完善**：补全遗留的占位功能

## 2. 当前项目状态评估

### 2.1 已完成的基础设施（迭代131-135）

| 能力 | 状态 | 证据 |
|------|------|------|
| 性能基准测试 | ✅ 完成 | 7个关键API测试 |
| 安全审计脚本 | ✅ 完成 | `scripts/security_scan.sh` |
| 监控告警配置 | ✅ 完成 | `config/alerting.yaml` |
| 静默失败处理 | ✅ 完成 | 业务逻辑有日志记录 |
| 前端组件测试 | ✅ 完成 | 4个页面测试文件 |
| API错误响应规范 | ✅ 完成 | 统一错误码格式 |
| 日志追踪增强 | ✅ 完成 | X-Request-ID |

### 2.2 识别的新问题与改进点

| 问题 | 优先级 | 影响 | 来源 |
|------|--------|------|------|
| 国际化未实现 | P1 | 多语言用户无法使用 | 迭代133建议 |
| 移动端适配不完善 | P1 | 移动用户体验差 | 迭代133建议 |
| 实盘风控机制待增强 | P1 | 资金安全风险 | 迭代133建议 |
| 历史行情数据未实现 | P2 | 功能缺失 | NotImplementedError |
| Docker沙箱模式占位 | P3 | 代码冗余 | 迭代133识别 |
| 大数据量性能待优化 | P2 | 回测结果加载慢 | 迭代133建议 |

---

## 3. 工作包定义

### WP1：国际化(i18n)实现（P1）

#### 目标
实现中英文双语支持，为后续扩展更多语言奠定基础。

#### 执行步骤

**1.1 安装i18n依赖**
```bash
cd src/frontend
npm install vue-i18n@9
```

**1.2 创建i18n配置**
```typescript
// src/frontend/src/i18n/index.ts
import { createI18n } from 'vue-i18n'
import zhCN from './locales/zh-CN'
import enUS from './locales/en-US'

const i18n = createI18n({
  legacy: false,
  locale: localStorage.getItem('locale') || 'zh-CN',
  fallbackLocale: 'en-US',
  messages: {
    'zh-CN': zhCN,
    'en-US': enUS
  }
})

export default i18n
```

**1.3 创建语言文件**
```
src/frontend/src/i18n/locales/
├── zh-CN.ts    # 中文
└── en-US.ts    # 英文
```

**1.4 语言切换组件**
```vue
<!-- components/LanguageSwitcher.vue -->
<template>
  <el-dropdown @command="changeLocale">
    <span class="el-dropdown-link">
      {{ currentLocaleLabel }}
    </span>
    <template #dropdown>
      <el-dropdown-menu>
        <el-dropdown-item command="zh-CN">中文</el-dropdown-item>
        <el-dropdown-item command="en-US">English</el-dropdown-item>
      </el-dropdown-menu>
    </template>
  </el-dropdown>
</template>
```

**1.5 优先翻译范围**
- 导航菜单
- 表单标签
- 按钮文字
- 错误提示
- 表格列头

#### 验收标准
- [ ] 中英文可切换
- [ ] 核心页面文字已翻译
- [ ] 语言设置持久化

---

### WP2：移动端响应式适配（P1）

#### 目标
优化移动端布局，确保核心功能在手机上可用。

#### 当前问题清单

| 页面 | 问题 | 改进方案 |
|------|------|----------|
| Dashboard | 图表在小屏挤压 | 使用堆叠布局 |
| BacktestPage | 表单字段过窄 | 单列布局 |
| LiveTradingPage | 表格横向滚动 | 卡片式展示 |
| StrategyPage | 代码编辑器过小 | 全屏编辑模式 |

#### 执行步骤

**2.1 添加响应式断点**
```scss
// src/frontend/src/styles/responsive.scss
$breakpoints: (
  'xs': 480px,   // 手机
  'sm': 768px,   // 平板
  'md': 992px,   // 小屏电脑
  'lg': 1200px   // 大屏电脑
);

@mixin respond-to($breakpoint) {
  @media (max-width: map-get($breakpoints, $breakpoint)) {
    @content;
  }
}
```

**2.2 优化核心页面布局**
- Dashboard：移动端堆叠卡片
- BacktestPage：表单单列布局
- LiveTradingPage：实例卡片化展示

**2.3 移动端导航优化**
- 使用抽屉式菜单替代侧边栏
- 底部导航栏快速切换

#### 验收标准
- [ ] 核心页面在375px宽度可用
- [ ] 无横向滚动问题
- [ ] 触摸操作友好

---

### WP3：实盘风控机制增强（P1）

#### 目标
增强实盘交易的风险控制能力，保护用户资金安全。

#### 功能设计

**3.1 风控规则配置**
```typescript
interface RiskControlConfig {
  max_position_pct: number      // 单品种最大仓位比例
  max_total_position_pct: number // 总仓位上限
  max_daily_loss_pct: number    // 日亏损上限
  max_drawdown_pct: number      // 最大回撤限制
  stop_loss_pct: number         // 止损比例
  take_profit_pct: number       // 止盈比例
}
```

**3.2 风控检查逻辑**
```python
# services/risk_control_service.py
class RiskControlService:
    async def check_position_limit(self, user_id: str, symbol: str, size: float) -> bool:
        """检查仓位限制"""
        pass
    
    async def check_daily_loss(self, user_id: str) -> bool:
        """检查日亏损是否超限"""
        pass
    
    async def trigger_stop_loss(self, position: Position) -> bool:
        """触发止损"""
        pass
```

**3.3 风险预警通知**
- 仓位超限预警
- 亏损预警
- 异常交易预警

#### 验收标准
- [ ] 风控规则可配置
- [ ] 仓位限制生效
- [ ] 止损止盈可触发

---

### WP4：历史行情数据功能补全（P2）

#### 目标
实现历史行情数据查询功能，或明确标记为"规划中"。

#### 当前状态
- `realtime_data_service.py` 中 `get_historical_data` 抛出 `NotImplementedError`
- API返回501状态码

#### 执行方案

**方案A：最小实现**
- 从本地CSV文件读取历史数据
- 支持日线级别查询
- 限制查询时间范围

**方案B：降级处理**
- 保持501响应
- 前端隐藏入口
- 文档明确说明"规划中"

#### 推荐：方案B
当前回测功能已支持历史数据，实时行情历史查询可作为后续扩展。

#### 验收标准
- [ ] 前端不暴露未实现功能
- [ ] API文档更新功能状态

---

### WP5：Docker沙箱模式清理（P3）

#### 目标
移除或明确标记Docker沙箱占位代码。

#### 当前状态
- `utils/sandbox.py` 中Docker模式抛出 `NotImplementedError`
- 代码冗余，增加维护负担

#### 执行步骤

**5.1 评估使用情况**
- 检查是否有用户依赖此功能
- 评估受限Python模式是否满足安全需求

**5.2 处理方案**
- 移除Docker模式入口
- 简化沙箱代码
- 更新文档说明

#### 验收标准
- [ ] 沙箱代码简化
- [ ] 无占位异常

---

### WP6：大数据量性能优化（P2）

#### 目标
优化回测结果大数据量场景的性能。

#### 当前问题
- 回测结果全量加载，大数据量时卡顿
- 交易记录无分页

#### 执行步骤

**6.1 后端分页支持**
```python
# api/backtest_enhanced.py
@router.get("/results/{task_id}/trades")
async def get_trades(
    task_id: str,
    page: int = Query(1, ge=1),
    page_size: int = Query(50, ge=1, le=500),
):
    """分页获取交易记录"""
    pass
```

**6.2 前端虚拟滚动**
```typescript
// 使用虚拟滚动优化大列表
import { useVirtualList } from '@vueuse/core'

const { list, containerProps, wrapperProps } = useVirtualList(
  trades,
  { itemHeight: 48 }
)
```

**6.3 增量加载**
- 图表数据按需加载
- 滚动加载更多

#### 验收标准
- [ ] 交易记录支持分页
- [ ] 图表渲染流畅（>1000数据点）

---

## 4. 执行顺序

```
Week 1: WP1 (国际化) + WP2 (移动端适配)
Week 2: WP3 (实盘风控) + WP6 (大数据量优化)
Week 3: WP4 (历史行情) + WP5 (沙箱清理) + 验收
```

## 5. 风险与控制

| 风险 | 影响 | 控制措施 |
|------|------|----------|
| i18n翻译工作量大 | 进度延迟 | 优先翻译核心页面 |
| 移动端适配复杂 | 兼容性问题 | 分阶段优化，先保证可用 |
| 风控逻辑复杂 | 开发周期长 | 先实现基础功能 |

## 6. 完成定义

本轮迭代在以下条件同时满足时完成：

1. [x] 中英文可切换，核心页面已翻译
2. [x] 核心页面在移动端可用
3. [x] 实盘风控基础功能生效
4. [x] 历史行情功能状态明确
5. [x] Docker沙箱占位代码清理

---

**创建时间**: 2026-03-25
**状态**: ✅ 已完成
**完成时间**: 2026-03-25
**执行周期**: 1天
