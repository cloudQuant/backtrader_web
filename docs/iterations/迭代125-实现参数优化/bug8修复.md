优化参数设置或者批量优化提交的时候，希望能把涉及到的策略单元的运行状态，已用时间，剩余时间都初始化一下，已用时间从策略单元优化开始计算，直到结束，记录使用的时间，初始化的时候显示0；剩余时间是预计还有多长时间，这个是估计值，初始化的时候也是0；运行状态初始化的时候是-，然后记录一下运行完成的次数/总次数，这个是原先在剩余时间中显示的。

修复下面的三个问题：

现在运行状态在开始的时候没有初始化，还都是已完成，需要修复；另外需要动态显示完成数/总数；

已用时间没有初始化为0，优化过程中也没有动态显示；

剩余时间这个初始化为0了，但是优化过程中也没有动态显示预计剩余多少s