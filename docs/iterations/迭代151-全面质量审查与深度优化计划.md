# 迭代 151 - 全面质量审查与深度优化计划

> **创建日期**: 2026-03-26
> **目标**: 基于全面代码审查结果，系统性修复安全隐患、提升代码质量、优化项目结构

---

## 审查发现摘要

| 类别 | 严重程度 | 发现数量 | 优先级 |
|------|----------|----------|--------|
| 安全凭证泄露 | 紧急 | 3 处 | P0 |
| 后端超大文件 | 高 | 10 个文件 | P1 |
| 前端超大组件 | 高 | 2 个组件 | P1 |
| 前端类型安全 | 中 | 30+ 处 any 类型 | P2 |
| 重复逻辑未提取 | 中 | 5 处图表 resize 模式 | P2 |
| 硬编码配置值 | 中 | 10+ 处 | P2 |
| CI/CD 配置问题 | 中 | 4 处 | P2 |
| 数据库迁移不完整 | 中 | 仅 2 个迁移 | P2 |
| 缺少 .dockerignore | 低 | 1 处 | P3 |
| 技术债务标记 | 低 | 16 处 TODO/FIXME | P3 |

---

## Phase 1: 安全修复 (P0 - 紧急)

### 任务 1.1: 清理仓库中的敏感凭证
- [ ] 移除 `strategies/simulate/a_td_sequential/.env` 中的 CTP 交易凭证
- [ ] 确保 `src/backend/.env` 不被 git 跟踪
- [ ] 更新 `.gitignore` 确保所有 `.env` 文件被排除
- [ ] 清理 `logs/` 目录下的 35+ 日志文件
- [ ] 检查并确认所有 `.env` 文件均通过 `.env.example` 模板管理

### 任务 1.2: 加强默认配置安全
- [ ] 移除 `config.py` 中的弱默认密码 (`admin123`)，改为启动时强制要求设置
- [ ] 确保 `SECRET_KEY` 和 `JWT_SECRET_KEY` 无法使用默认值启动生产环境

---

## Phase 2: 后端代码拆分 (P1)

### 任务 2.1: 拆分超大服务文件

**monitoring_service.py (1008 行 -> 4 个文件):**
- [ ] 提取 `AlertRuleService` -> `alert_rule_service.py`
- [ ] 提取 `NotificationDispatcher` -> `notification_service.py`
- [ ] 提取 `TriggerEvaluator` -> `trigger_evaluator.py`
- [ ] 保留 `MonitoringService` 作为编排层

**strategy_version_service.py (950 行 -> 3 个文件):**
- [ ] 提取 `BranchService` -> `strategy_branch_service.py`
- [ ] 提取 `ComparisonService` -> `strategy_comparison_service.py`
- [ ] 保留核心版本控制逻辑

**paper_trading_service.py (775 行 -> 2 个文件):**
- [ ] 提取 `OrderManagementService` -> `paper_order_service.py`
- [ ] 保留账户和仓位管理逻辑

### 任务 2.2: 提取公共模式
- [ ] 创建 `BaseService` 基类，抽取通用的 `list_*()` 分页/过滤逻辑
- [ ] 创建 `OwnershipMixin`，统一 `user_id` 权限检查模式
- [ ] 将硬编码时间间隔 (300s, 30s, 60s) 移至 `config.py`

### 任务 2.3: 拆分超大测试文件
- [ ] `test_live_trading_manager.py` (1340 行) -> 按功能拆分为 3-4 个文件
- [ ] `test_paper_trading_api.py` (1231 行) -> 按资源类型拆分
- [ ] `test_backtest_enhanced.py` (1153 行) -> 按测试场景拆分
- [ ] 修复脆弱的 `patch("asyncio.sleep", side_effect=Exception)` 测试模式

---

## Phase 3: 前端优化 (P1-P2)

### 任务 3.1: 拆分超大 Vue 组件
- [ ] `LiveTradingPage.vue` (956 行) -> 提取逻辑到 `useLiveTrading` composable + 子组件
- [ ] `OptimizationPage.vue` (884 行) -> 提取分页/参数管理到 `useOptimization` composable

### 任务 3.2: 提取公共 composable
- [ ] 创建 `useChartResize()` composable，替代 5 处重复的图表 resize 监听逻辑
  - EquityCurve.vue
  - TradeSignalChart.vue
  - ReturnHeatmap.vue
  - KlineChart.vue
  - DrawdownChart.vue

### 任务 3.3: 类型安全修复
- [ ] 替换测试文件中 30+ 处 `as any` 为正确的类型定义
- [ ] 修复 `App.vue` 中的 `@ts-expect-error` Element Plus locale 类型问题
- [ ] 为 mock API 响应创建专用的测试类型接口

### 任务 3.4: 完成国际化
- [ ] 移除 `useInstanceActions.ts` 中硬编码的中文状态标签
- [ ] 统一错误消息的 i18n 处理

---

## Phase 4: 基础设施优化 (P2)

### 任务 4.1: 依赖管理规范化
- [ ] 移除 `requirements.txt`，统一使用 `pyproject.toml` 管理依赖
- [ ] 将 `fincore` 改为 optional dependency（已有 fallback 机制）

### 任务 4.2: 数据库迁移补全
- [ ] 审计现有 schema 变更历史，补充缺失的 alembic 迁移
- [ ] 修复 `alembic.ini` 中硬编码的数据库 URL，改为环境变量驱动

### 任务 4.3: CI/CD 改进
- [ ] 移除前端构建中的 `--skipLibCheck` 以提升类型安全
- [ ] 添加 `--cov-fail-under` pytest 覆盖率门槛
- [ ] 将 CI 测试数据库凭证改为随机生成

### 任务 4.4: Docker 改进
- [ ] 创建 `.dockerignore` 文件排除 `logs/`, `datas/`, `__pycache__/` 等
- [ ] 创建生产环境 `docker-compose.yml`

---

## Phase 5: 项目整理 (P3)

### 任务 5.1: 文档归档
- [ ] 将迭代 123-140 的计划文档归档到 `docs/iterations/archived/`
- [ ] 更新 `docs/iterations/README.md` 反映当前状态

### 任务 5.2: 大文件外部化
- [ ] 将 `datas/` 目录下的大文件 (>10MB) 移出仓库，使用 Git LFS 或外部存储
- [ ] 创建 `scripts/download_sample_data.sh` 按需获取测试数据

### 任务 5.3: 提交未跟踪的重要文件
- [ ] 提交 `CHANGELOG.md`
- [ ] 提交 `config/alerting.yaml`
- [ ] 提交 `src/frontend/src/i18n/` 国际化目录

### 任务 5.4: 清理技术债务
- [ ] 逐一审查并解决 16 处 TODO/FIXME 标记
- [ ] 移除 `live_trading_manager.py` 中的向后兼容包装函数
- [ ] 移除 `schemas/__init__.py` 中的 star imports

---

## 验收标准

- [ ] 无任何敏感凭证存在于代码库中
- [ ] 所有后端服务文件 < 500 行
- [ ] 所有前端组件文件 < 800 行
- [ ] 前端测试中无 `as any` 使用
- [ ] CI 管道完全通过
- [ ] 后端测试覆盖率 >= 90%
- [ ] 所有图表组件使用统一的 `useChartResize` composable

---

## 执行顺序

```
Phase 1 (安全) → Phase 2 (后端拆分) → Phase 3 (前端优化) → Phase 4 (基础设施) → Phase 5 (整理)
```

> **注意**: Phase 1 必须最先完成，其余 Phase 可根据情况并行推进。
