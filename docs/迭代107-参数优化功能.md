在左侧回测分析下面，增加一个参数优化功能，希望能够在选择策略之后，能够显示现在默认的参数，并且可以设置开始值，结束值，步长，并且可以选择优化使用的进程数，然后点击优化，可以多进程进行优化。

优化过程中需要展示优化的进度。

优化完成之后，需要能够展示优化的结果，包括不同的参数列，年化收益率，夏普率，最大回撤，交易次数等关键的绩效分析指标，形成一个策略优化结果的列表；

可以选中两个参数，和一个性能指标形成一个热力图；

可以选中一个参数，和 一个性能指标形成箱体图；

可以选中三个参数，和一个性能指标形成一个三维的散点图；

按照行业的最佳实践，完成上面的任务。