# DEPRECATED — kept only for legacy scripts and external automation.
#
# `pyproject.toml` is the single source of truth for backend dependencies.
# `scripts/check_deps_sync.py` will WARN if this file drifts out of sync
# but will no longer fail CI.
#
# Recommended install (covers everything):
#     pip install -e ".[dev,postgres,mysql,redis,backtrader,data]"
#
# Minimal install (runtime only):
#     pip install -e ".[backtrader]"
#
# If you find yourself editing this file, please update `pyproject.toml`
# instead and regenerate this file with:
#     pip-compile --extra=dev --extra=postgres --extra=mysql --extra=redis \
#                 --extra=backtrader --extra=data \
#                 -o requirements.txt pyproject.toml
#
# Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
apscheduler>=3.10.4
slowapi>=0.1.9

# 数据验证
pydantic>=2.5.0
email-validator>=2.0.0
pydantic-settings>=2.1.0
jinja2>=3.1.0
python-multipart>=0.0.6

# 数据库
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0  # PostgreSQL
aiomysql>=0.2.0  # MySQL
mysql-connector-python>=8.0.0
PyMySQL>=1.2.0

# 认证
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# 日志
loguru>=0.7.2

# 缓存（可选）
redis>=5.0.0

# 回测引擎
backtrader>=1.9.78.123

# 配置
PyYAML>=6.0.1

# 数据处理
pandas>=2.1.0
numpy>=1.26.0

# 数据源与指标
akshare>=1.12.0
fincore>=0.3.0; python_version >= "3.11"

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# 开发工具
ruff>=0.1.0
