# Iteration 197 rollout state is server-owned.
#
# Authenticated clients read GET /api/v1/data/market-data/capabilities after
# data:read authorization. Do not add browser VITE flags to enable, disable,
# or hide the market-data v2, bundle, research bridge, or cache-fill paths.
