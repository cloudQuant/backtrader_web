{
  "_comment": "Iteration 177 §E mypy repo-wide ratchet baseline. CI fails when `mypy app` reports MORE errors than baseline_errors. Lower this number whenever the count drops (the ratchet only turns one way). Regenerate with the pinned mypy version below via --update. 2026-08-15 refresh: the lint job now installs from the pinned dev lock (deterministic, same resolution as backend-test canonical); +34 vs the 2026-07-26 baseline is dependency drift plus the iteration 190/192 feature modules. A per-file mypy diff at identical dependencies confirmed the iteration 193 working tree adds zero new errors; cleanup of the historical count is registered in iteration 194 Task I.",
  "baseline_errors": 759,
  "mypy_version": "mypy 1.20.2 (compiled: yes)",
  "baseline_date": "2026-08-15"
}
