#!/bin/bash

###############################################################################
# Backtrader Web - 停止脚本 (根目录快捷方式)
# 此脚本用于快速停止前端和后端服务
###############################################################################

# 获取脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 调用 scripts 目录下的停止脚本
bash "$SCRIPT_DIR/scripts/stop_app.sh" "$@"
